#ifndef UVCORE_JSONCPP_WRITER_H
#define UVCORE_JSONCPP_WRITER_H

/*

#include <json/json.h>
#include <sstream>

class JsonWriter
{
public:
	JsonWriter(const Json::Value&, bool is_indent = true);

	void to_string(std::string& out);

private:
	void pushValue(const std::string&);
	void writeArrayValue2(Json::Value const& value);

	void writeIndent();
	void writeWithIndent(std::string const& value);
private:
	bool _is_indent;
	const Json::Value& _json;
	std::stringstream ss;
};
*/

#endif